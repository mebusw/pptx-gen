# pptx-gen SKILL

using pptxgenjs library to generate contents of powerpoint slides.

## Installation

```bash
npx skills add https://github.com/mebusw/pptx-gen.git
```
You need nodejs 22+ to run .mjs file.

```bash
cd scripts/
pnpm i 2>&1 || npm i
```